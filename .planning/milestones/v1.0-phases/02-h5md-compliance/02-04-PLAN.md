---
phase: 02-h5md-compliance
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - tests/test_h5md_backend.py
autonomous: true
requirements: [H5MD-03, H5MD-04]

must_haves:
  truths:
    - "Files written by znh5md can be read by H5MDBackend"
    - "Files written by H5MDBackend can be read by znh5md"
    - "Auto-infer variable_shape works: fixed-shape data has no padding overhead, upgrading to variable works"
    - "Constraints round-trip through H5MDBackend (write ASE constraints, read them back)"
    - "Unit attributes present on value datasets (positions=Angstrom, forces=eV/Angstrom, energy=eV)"
    - "file_handle parameter works with H5MDBackend"
  artifacts:
    - path: "tests/test_h5md_backend.py"
      provides: "New tests for auto-infer variable_shape, constraints, units, file_handle"
      contains: "test_auto_infer"
  key_links:
    - from: "tests/test_h5md_backend.py"
      to: "src/asebytes/h5md/_backend.py"
      via: "imports and exercises H5MDBackend"
      pattern: "from asebytes.h5md._backend import H5MDBackend"
    - from: "tests/test_h5md_backend.py"
      to: "znh5md"
      via: "cross-import interop tests"
      pattern: "import znh5md"
---

<objective>
Add new tests for auto-infer variable_shape, constraint round-trip, unit attributes, and file_handle support. Verify existing znh5md cross-compatibility tests still pass.

Purpose: The H5MDBackend rewrite (Plan 03) must be verified with new tests for new features (auto-infer, constraints, units, file_handle) and confirmed interop with znh5md. Existing tests validate regression-free behavior; new tests validate the new capabilities.
Output: Extended tests/test_h5md_backend.py with new test cases.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-h5md-compliance/02-CONTEXT.md
@.planning/phases/02-h5md-compliance/02-RESEARCH.md
@.planning/phases/02-h5md-compliance/02-03-SUMMARY.md

<interfaces>
From src/asebytes/h5md/_backend.py (rewritten in Plan 03):
```python
class H5MDBackend(PaddedColumnarBackend):
    def __init__(self, file=None, *, file_handle=None, file_factory=None,
                 group=None, readonly=False, compression="gzip",
                 compression_opts=None, chunk_frames=64,
                 pbc_group=True, author_name="N/A", author_email=None): ...

    def extend(self, data: dict[str, list]): ...
    def get(self, idx: int) -> dict[str, Any]: ...
    def length(self) -> int: ...
    def keys(self, index: int) -> list[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add new feature tests for H5MDBackend</name>
  <files>tests/test_h5md_backend.py</files>
  <behavior>
  New test class TestAutoInferVariableShape:
  - test_fixed_shape_no_padding: Write 2 batches of same-size atoms (3 atoms each). Verify no NaN padding in the HDF5 file (position dataset shape axis-1 == 3, not larger).
  - test_auto_upgrade_to_variable: Write batch of 3-atom frames, then batch of 5-atom frames. Verify position dataset axis-1 == 5, and first batch's data is padded with NaN on axis-1 positions 3-4.
  - test_mixed_in_single_batch: Write single batch with frames of 3 and 5 atoms. Verify variable_shape detected immediately.

  New test class TestConstraintRoundTrip:
  - test_write_read_constraints: Create Atoms with FixAtoms constraint, write via extend, read back via get, verify constraint type and indices match.
  - test_empty_constraints: Write Atoms without constraints, read back, verify no constraints.
  - test_multiple_constraints: Write Atoms with FixAtoms + FixedLine, verify both come back.

  New test class TestUnitAttributes:
  - test_position_unit: Write atoms, open HDF5 file directly, verify particles/{grp}/position/value has attrs["unit"]=="Angstrom".
  - test_force_unit: Write atoms with calc forces, verify force/value has attrs["unit"]=="eV/Angstrom".
  - test_energy_unit: Write atoms with calc energy, verify observables/{grp}/potential_energy/value has attrs["unit"]=="eV".

  New test class TestFileHandle:
  - test_write_with_file_handle: Open h5py.File manually, create H5MDBackend(file_handle=f), write data, close backend, verify data readable.
  - test_read_with_file_handle: Write normally, then reopen with file_handle=h5py.File(..., "r"), verify can read back.
  </behavior>
  <action>
  Add these test classes to the existing tests/test_h5md_backend.py file (append, don't modify existing tests).

  Use ASE Atoms objects with SinglePointCalculator for calc results. Use ase.constraints.FixAtoms and ase.constraints.FixedLine for constraint tests.

  For auto-infer tests, inspect the raw HDF5 file structure to verify padding behavior (open with h5py directly after H5MDBackend writes).

  For unit tests, open the written file with h5py and check dataset attrs.

  For file_handle tests, use h5py.File directly and pass to H5MDBackend(file_handle=...).
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/asebytes && uv run pytest tests/test_h5md_backend.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All new tests pass: auto-infer variable_shape (3 tests), constraint round-trip (3 tests), unit attributes (3 tests), file_handle (2 tests). All existing 51 tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify znh5md cross-compatibility and run full suite</name>
  <files>tests/test_h5md_backend.py</files>
  <action>
  Run the existing znh5md cross-compatibility tests specifically:
  ```
  uv run pytest tests/test_h5md_backend.py::TestZnH5MDCompat -x -v
  ```

  If any interop test fails, diagnose and fix:
  - Check that files written by H5MDBackend have the exact H5MD group structure znh5md expects
  - Check that ORIGIN_ATTR values match znh5md's ORIGIN_TYPE ("calc", "info", "arrays", "atoms")
  - Check that _n_atoms is stored under asebytes/{grp}/ (not particles/)
  - Check that box/boundary attribute exists alongside box/edges and box/pbc

  Then run the complete test suite to verify no regressions across the entire codebase:
  ```
  uv run pytest tests/ -x -q
  ```
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/asebytes && uv run pytest tests/test_h5md_backend.py::TestZnH5MDCompat -x -v && uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All znh5md cross-compatibility tests pass (files written by either tool readable by the other). Full test suite green. Phase 2 complete.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_h5md_backend.py -x -q` -- all tests pass (51 existing + ~11 new)
- `uv run pytest tests/test_h5md_backend.py::TestZnH5MDCompat -x -v` -- interop specifically verified
- `uv run pytest tests/ -x -q` -- full suite green
</verification>

<success_criteria>
New feature tests demonstrate auto-infer variable_shape, constraint serialization, unit attributes, and file_handle support all work correctly. znh5md cross-compatibility confirmed. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-h5md-compliance/02-04-SUMMARY.md`
</output>
