---
phase: 03-contract-test-suite
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tests/contract/test_async_blob_contract.py
  - tests/contract/test_async_object_contract.py
  - tests/contract/test_async_ase_contract.py
  - tests/contract/test_h5md_compliance.py
autonomous: true
requirements: [TEST-03, TEST-04, QUAL-06]

must_haves:
  truths:
    - "Every read-write backend is exercised through AsyncBlobIO, AsyncObjectIO, AsyncASEIO facades"
    - "All async tests use @pytest.mark.anyio consistently"
    - "H5MD file structure matches H5MD 1.1 spec (particles group, step/time/value)"
    - "Files written by asebytes are readable by znh5md and vice versa"
  artifacts:
    - path: "tests/contract/test_async_blob_contract.py"
      provides: "AsyncBlobIO facade contract tests"
      contains: "pytest.mark.anyio"
    - path: "tests/contract/test_async_object_contract.py"
      provides: "AsyncObjectIO facade contract tests"
      contains: "pytest.mark.anyio"
    - path: "tests/contract/test_async_ase_contract.py"
      provides: "AsyncASEIO facade contract tests"
      contains: "pytest.mark.anyio"
    - path: "tests/contract/test_h5md_compliance.py"
      provides: "H5MD spec compliance and znh5md interop tests"
      contains: "h5md"
  key_links:
    - from: "tests/contract/conftest.py"
      to: "tests/contract/test_async_ase_contract.py"
      via: "async fixture providing AsyncASEIO"
      pattern: "async_aseio"
    - from: "tests/contract/test_h5md_compliance.py"
      to: "src/asebytes/h5md/"
      via: "H5MD file structure verification with h5py"
      pattern: "h5py.File"
---

<objective>
Create async contract test mirrors and H5MD compliance tests. Complete the contract suite with async facades exercising all backends via SyncToAsyncAdapter, and H5MD-specific tests verifying spec compliance and znh5md interop.

Purpose: Ensure async facades work identically to sync, verify H5MD standard compliance, and standardize all async markers to @pytest.mark.anyio (QUAL-06).
Output: Three async test files and one H5MD compliance test file in `tests/contract/`.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contract-test-suite/03-CONTEXT.md
@.planning/phases/03-contract-test-suite/03-RESEARCH.md
@.planning/phases/03-contract-test-suite/03-01-SUMMARY.md

@tests/contract/conftest.py
@tests/contract/test_ase_contract.py

<interfaces>
<!-- Async facade constructors -->

From src/asebytes/_async_blob_io.py:
```python
class AsyncBlobIO:
    def __init__(self, backend_or_path: str, *, readonly=None): ...
    async def extend(self, items): ...
    async def __getitem__(self, index): ...
    async def len(self): ...
```

From src/asebytes/_async_object_io.py:
```python
class AsyncObjectIO:
    def __init__(self, backend_or_path: str, *, readonly=None): ...
    async def extend(self, items): ...
    async def __getitem__(self, index): ...
    async def len(self): ...
```

From src/asebytes/_async_io.py:
```python
class AsyncASEIO:
    def __init__(self, path_or_backend, *, readonly=None): ...
    async def extend(self, atoms_list): ...
    async def __getitem__(self, index): ...
    async def len(self): ...
```

Note: Async facades use `await` for all operations. `len()` is a method (not __len__) since __len__ can't be async.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create async facade contract tests</name>
  <files>tests/contract/conftest.py, tests/contract/test_async_blob_contract.py, tests/contract/test_async_object_contract.py, tests/contract/test_async_ase_contract.py</files>
  <action>
1. Update `tests/contract/conftest.py` to add async fixtures:
   - Add `ASYNC_ASEIO_BACKENDS`, `ASYNC_OBJECTIO_BACKENDS`, `ASYNC_BLOBIO_BACKENDS` param lists (same backends, same marks as sync lists).
   - Add async fixtures:
     - `async_aseio(tmp_path, request)` -> `AsyncASEIO(factory(tmp_path))`
     - `async_objectio(tmp_path, request)` -> `AsyncObjectIO(factory(tmp_path))`
     - `async_blobio(tmp_path, request)` -> `AsyncBlobIO(factory(tmp_path))`
   - Import `AsyncASEIO` from `asebytes._async_io`, `AsyncObjectIO` from `asebytes._async_object_io`, `AsyncBlobIO` from `asebytes._async_blob_io`.

2. Create `tests/contract/test_async_blob_contract.py`:
   - All tests decorated with `@pytest.mark.anyio` (QUAL-06).
   - Mirror the sync blob contract tests from test_blob_contract.py:
     - `test_extend_and_len(async_blobio)`: await extend, await len()
     - `test_get_by_index(async_blobio)`: await extend, result = await async_blobio[0]
     - `test_slice(async_blobio)`: await extend, result = await async_blobio[0:2]
     - `test_negative_index(async_blobio)`: await extend, await async_blobio[-1]
     - `test_iteration(async_blobio)`: await extend, async for item in async_blobio
     - `test_keys(async_blobio)`: await extend, await keys(0)

3. Create `tests/contract/test_async_object_contract.py`:
   - All tests decorated with `@pytest.mark.anyio`.
   - Mirror sync object contract tests:
     - `test_extend_and_len(async_objectio)`: await extend, await len()
     - `test_get_by_index(async_objectio)`: await extend, await async_objectio[0]
     - `test_slice(async_objectio)`: await extend, await async_objectio[0:2]
     - `test_negative_index(async_objectio)`: await extend, await async_objectio[-1]
     - `test_iteration(async_objectio)`: await extend, async for
     - `test_keys(async_objectio)`: await extend, await keys(0)
     - `test_column_access(async_objectio)`: await extend, await get_column("key")

4. Create `tests/contract/test_async_ase_contract.py`:
   - All tests decorated with `@pytest.mark.anyio`.
   - Mirror the core sync ASEIO contract tests:
     - `test_extend_and_len(async_aseio, s22)`: await extend, await len()
     - `test_single_frame_roundtrip(async_aseio, simple_atoms)`: await extend, await read, assert_atoms_equal
     - `test_multi_frame_roundtrip(async_aseio, ethanol)`: await extend, read first/middle/last
     - `test_getitem_by_index(async_aseio, s22)`: await extend, await async_aseio[0]
     - `test_getitem_negative_index(async_aseio, s22)`: await extend, await async_aseio[-1]
     - `test_slice(async_aseio, s22)`: await extend, await async_aseio[0:5]
     - `test_iteration(async_aseio, s22)`: await extend, async for, count
     - `test_variable_particle_count(async_aseio, s22)`: marked `@pytest.mark.supports_variable_particles`
     - `test_info_roundtrip(async_aseio, atoms_with_info)`: marked `@pytest.mark.supports_nested_info`
     - `test_calc_roundtrip(async_aseio, atoms_with_calc)`: extend, read, assert_atoms_equal

CRITICAL: Every async test function MUST have `@pytest.mark.anyio` decorator. No `@pytest.mark.asyncio` allowed. This satisfies QUAL-06.

Note on async iteration: Check if AsyncASEIO supports `async for`. If it uses `__aiter__`/`__anext__`, use that. If not, iterate manually with index.
  </action>
  <verify>
    <automated>uv run pytest tests/contract/test_async_blob_contract.py tests/contract/test_async_object_contract.py tests/contract/test_async_ase_contract.py -x -m "not mongodb and not redis" --timeout=120 2>&1 | tail -20</automated>
  </verify>
  <done>All async contract tests pass for file-based + memory backends. Every async test uses @pytest.mark.anyio. Async facades exercise same backends as sync tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create H5MD compliance and interop tests</name>
  <files>tests/contract/test_h5md_compliance.py</files>
  <action>
Create `tests/contract/test_h5md_compliance.py` with H5MD 1.1 spec compliance tests and znh5md interop tests.

1. H5MD file structure compliance tests (use h5py to inspect file structure after writing):
   - `test_h5md_root_attributes(tmp_path)`: Write Atoms via ASEIO to .h5md, open with h5py, verify root group has `h5md` attribute group with `version` attribute == [1, 1] and `author` and `creator` groups.
   - `test_h5md_particles_group(tmp_path, s22)`: Write s22, inspect with h5py. Verify `particles/` group exists, contains at least one subgroup, each subgroup has `position/value`, `position/step`, `position/time`, `species/value`.
   - `test_h5md_time_dependent_structure(tmp_path, s22)`: Write s22, verify position/value dataset shape is (N_frames, max_atoms, 3), step dataset is (N_frames,), time dataset is (N_frames,).
   - `test_h5md_box_group(tmp_path, s22_mixed_pbc_cell)`: Write s22_mixed_pbc_cell, verify `box` group exists under particles group with `edges` and `boundary` attributes/datasets per H5MD spec.

2. znh5md interop tests:
   - `test_znh5md_reads_asebytes_file(tmp_path, s22_energy_forces)`: Write s22_energy_forces via ASEIO(".h5md"), read with `znh5md.IO(path)`, compare frame count and first frame's positions/numbers/energy.
   - `test_asebytes_reads_znh5md_file(tmp_path, s22_energy_forces)`: Write s22_energy_forces via `znh5md.IO(path, save_every=1)`, read with ASEIO(".h5md"), compare frame count and first frame's positions/numbers/energy.
   - `test_interop_roundtrip_preserves_forces(tmp_path, s22_energy_forces)`: Write with asebytes, read with znh5md, verify forces arrays match with np.allclose.

3. H5MD-specific edge cases:
   - `test_h5md_variable_particles(tmp_path, s22)`: Write s22 (variable particle counts), read back, verify each frame has correct atom count.
   - `test_h5md_constraints_roundtrip(tmp_path, atoms_with_constraints)`: Write atoms with constraints via H5MD, read back, verify constraints preserved.

Use `from asebytes import ASEIO` for writing/reading. Use `import h5py` for structure inspection. Use `import znh5md` for interop. All test data is synthetic (s22, ethanol from fixtures).
  </action>
  <verify>
    <automated>uv run pytest tests/contract/test_h5md_compliance.py -x --timeout=60 2>&1 | tail -20</automated>
  </verify>
  <done>H5MD compliance tests verify file structure matches H5MD 1.1 spec. znh5md interop tests verify cross-tool read/write produces equivalent data. All H5MD edge cases covered.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/contract/test_async_*.py -x -m "not mongodb and not redis"` passes
- `uv run pytest tests/contract/test_h5md_compliance.py -x` passes
- `grep -r "pytest.mark.asyncio" tests/contract/` returns NO results (QUAL-06)
- `grep -r "pytest.mark.anyio" tests/contract/test_async_*.py` returns results for every async test file
- `uv run pytest tests/contract/ -x -m "not mongodb and not redis" --timeout=120` full suite passes
</verification>

<success_criteria>
- Async mirrors exist for all three facades (BlobIO, ObjectIO, ASEIO)
- Every async test uses @pytest.mark.anyio (zero uses of @pytest.mark.asyncio)
- H5MD compliance tests verify file structure against H5MD 1.1 spec
- znh5md interop tests verify bidirectional read/write compatibility
- All tests pass for file-based + memory backends
</success_criteria>

<output>
After completion, create `.planning/phases/03-contract-test-suite/03-02-SUMMARY.md`
</output>
