---
phase: 03-contract-test-suite
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - tests/test_universal_blob_backend.py
  - tests/test_universal_object_backend.py
  - tests/test_aseio.py
  - tests/test_bytesio.py
  - tests/test_async_aseio.py
  - tests/test_async_blob_io.py
  - tests/test_async_object_io.py
  - tests/test_async_aseio_atoms.py
  - tests/test_h5md_backend.py
  - tests/conftest.py
autonomous: true
requirements: [TEST-01, TEST-09]

must_haves:
  truths:
    - "Overlapping old test files are deleted without losing unique test coverage"
    - "Full test suite still passes after cleanup"
    - "No test uses pytest.importorskip for required dependencies"
  artifacts:
    - path: "tests/"
      provides: "Clean test directory with contract suite as primary coverage source"
  key_links:
    - from: "tests/contract/"
      to: "tests/"
      via: "Contract suite subsumes deleted per-backend tests"
---

<objective>
Delete overlapping test files that are now subsumed by the contract suite, remove pytest.importorskip patterns for required dependencies, and clean up the root conftest.py of fixtures that moved to contract conftest.

Purpose: Eliminate duplicate test coverage and enforce TEST-09 (fail, not skip) policy across remaining tests.
Output: Leaner test directory with contract suite as the primary source of truth for backend correctness.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contract-test-suite/03-CONTEXT.md
@.planning/phases/03-contract-test-suite/03-01-SUMMARY.md
@.planning/phases/03-contract-test-suite/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete overlapping tests and remove importorskip patterns</name>
  <files>tests/</files>
  <action>
1. Analyze each existing test file for overlap with the contract suite. Delete files that are FULLY subsumed. The contract suite covers: extend/get/set/slice/iteration/keys/len/remove for all backends through all facades, plus edge cases (variable particles, info, calc, pbc, constraints, per-atom arrays), plus async mirrors, plus H5MD compliance.

   Files to DELETE (fully subsumed by contract suite):
   - `tests/test_universal_blob_backend.py` — parametrized blob tests, now in test_blob_contract.py
   - `tests/test_universal_object_backend.py` — parametrized object tests, now in test_object_contract.py
   - `tests/test_aseio.py` — ASEIO CRUD tests, now in test_ase_contract.py
   - `tests/test_bytesio.py` — BlobIO CRUD tests, now in test_blob_contract.py
   - `tests/test_bytesio_update.py` — BlobIO update tests, subsumed by contract set/overwrite tests
   - `tests/test_async_aseio.py` — async ASEIO tests, now in test_async_ase_contract.py
   - `tests/test_async_blob_io.py` — async BlobIO tests, now in test_async_blob_contract.py
   - `tests/test_async_object_io.py` — async ObjectIO tests, now in test_async_object_contract.py
   - `tests/test_async_aseio_atoms.py` — async atoms round-trip, now in test_async_ase_contract.py
   - `tests/test_h5md_backend.py` — H5MD tests, now in test_h5md_compliance.py + test_ase_contract.py

   Files to KEEP (test unique features NOT in contract suite):
   - `tests/test_adapters.py` — adapter wrapping logic
   - `tests/test_aseio_views.py` — view-specific behavior
   - `tests/test_aseio_update.py` — update semantics
   - `tests/test_cache_to.py` — cache-to pattern
   - `tests/test_mongodb.py` — MongoDB-specific features (group isolation, URI parsing)
   - `tests/test_redis.py` — Redis-specific features
   - `tests/test_memory_backend.py`, `tests/test_memory_uri.py` — memory-specific
   - `tests/test_registry_*.py` — registry-specific logic
   - All view tests, protocol tests, column tests, etc.

2. Search remaining test files for `pytest.importorskip` patterns:
   ```
   grep -r "importorskip" tests/
   ```
   For any found in tests that use pymongo or redis:
   - Replace `pytest.importorskip("pymongo")` with `import pymongo`
   - Replace `pytest.importorskip("redis")` with `import redis`
   - This ensures TEST-09: tests FAIL when dependencies missing, not skip.

   For `pytest.importorskip("h5py")` or `pytest.importorskip("lmdb")` -- these are optional extras, keep importorskip only if the test file is genuinely for optional-dependency testing (e.g., test_optional_deps.py). Otherwise replace with direct import.

3. Remove the `uni_blob_backend` and `uni_object_backend` fixtures and their factory functions from `tests/conftest.py` since they are superseded by the contract conftest. Keep all atom fixtures (s22, ethanol, atoms_with_*, etc.) and service URI fixtures (mongo_uri, redis_uri).

4. Run full test suite to verify nothing broke.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -m "not mongodb and not redis and not benchmark and not hf" --timeout=300 2>&1 | tail -30</automated>
  </verify>
  <done>Overlapping test files deleted. No pytest.importorskip for required dependencies (pymongo, redis). Full test suite passes. Root conftest.py cleaned of superseded fixtures.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -m "not mongodb and not redis and not benchmark and not hf" --timeout=300` passes
- `grep -r "importorskip.*pymongo\|importorskip.*redis" tests/` returns no results
- Deleted test files no longer exist on disk
- `tests/conftest.py` no longer contains `uni_blob_backend` or `uni_object_backend`
</verification>

<success_criteria>
- 10+ overlapping test files deleted
- No regressions in remaining tests
- TEST-09 enforced: no importorskip for required dependencies
- Clean test directory with contract suite as primary coverage source
</success_criteria>

<output>
After completion, create `.planning/phases/03-contract-test-suite/03-03-SUMMARY.md`
</output>
